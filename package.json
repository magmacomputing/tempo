{
  "name": "tempo",
  "version": "1.0.0",
  "description": "A Wrapper around the Javascript Temporal object",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "vitest run",
    "prebuild": "rm -rf web && mkdir web",
    "build": "tsc --build && ./node_modules/.bin/import-remap --from dist --to web --recursive",
    "postbuild": "rm -rf dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/magmacomputing/tempo.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "bugs": {
    "url": "https://github.com/magmacomputing/tempo/issues"
  },
  "homepage": "https://github.com/magmacomputing/tempo#readme",
  "dependencies": {
    "@js-temporal/polyfill": "^0.4.4",
    "@vitest/ui": "^1.2.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "import-remap": "^0.6.0",
    "vite-tsconfig-paths": "^4.3.1",
    "vitest": "^1.2.1"
  }
}
