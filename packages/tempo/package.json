{
  "name": "@magma/tempo",
  "version": "1.2.0",
  "description": "The Tempo core library",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "imports": {
    "#library": {
      "development": "../library/src/index.js",
      "default": "@magma/library"
    },
    "#library/*": {
      "development": "../library/src/*",
      "default": "@magma/library/*"
    },
    "#tempo": {
      "development": "./src/index.js",
      "default": "./dist/index.js"
    },
    "#tempo/*": {
      "development": "./src/*",
      "default": "./dist/*"
    }
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./enums": {
      "types": "./dist/tempo.enum.d.ts",
      "import": "./dist/tempo.enum.js"
    },
    "./plugins/ticker": {
      "types": "./dist/plugins/plugin.ticker.d.ts",
      "import": "./dist/plugins/plugin.ticker.js"
    }
  },
  "scripts": {
    "test": "vitest run",
    "repl": "tsx -i --import ./bin/setup.ts --import ./bin/repl.ts",
    "build": "tsc -b && npm run build:bundle && bash bin/resolve-self.sh",
    "build:bundle": "rollup -c",
    "clean": "tsc -b --clean",
    "publish": "npm publish --access public",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist/",
    "src/"
  ],
  "dependencies": {
    "@magma/library": "1.2.0",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "@js-temporal/polyfill": "^0.5.1",
    "@rollup/plugin-alias": "^6.0.0"
  }
}
